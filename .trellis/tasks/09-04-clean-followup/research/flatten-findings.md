# 车型 39「摊平」核查结论(2026-09-04,首次核查 + 重同步复测)

## 首次核查结论(基于重同步前数据)

疑似摊平的表象:car_param 2144 行 × 4 版本值,clean 6432 行;group 136 行/17 名;version 40 行/4 名。

**根因确认(两层叠加,均非清洗层摊平)**:

1. **官网接口按模块重复推送**(历史现象,2026-09-02 采集):goodsParams 对同一车型返回 136 个 configs(17 名 × 8 次),每重复一组参数名也重复 → car_param 2144 行;getGoodsAttrList 返回 40 行版本(4 名 × 10 次)。清洗映射 1:1 无摊平,是**上游冗余**。
2. **逻辑删除导致重同步行堆积**:persistParams 的「先清后插」因实体 @TableLogic 实际执行逻辑删(deleted=1),旧 136 组/2144 参数仍留在库中,新 17 组叠加 → group 153 行、param 2412 行(2144 旧 + 268 新)。

## 复测证据(2026-09-04 13:01 重同步车型 39 后)

- **官网现状接口已正常**:直连 goodsParams(goodsId=156)返回 configs=17 组、0 重复 → 9-02 的 ×8 重复是官网当时的临时行为(或旧接口形态),当前上游已干净。
- 新数据(去重+新采集):group 17 行、param 268 行、sum(版本值)=1072、clean **1072 行 = 1:1 精确对齐**(RULE 100%,fallbackPct=0)——**去重逻辑生效,清洗行数完全合理**(AC2 达成)。
- version 重同步后 4 行(deleted=0)。
- 参数块 28 块全部有向量,首行「车型:大唐EV」。

## 遗留(低优先,记录不处理)

- 存量逻辑删行(136 旧组、2144 旧参数、40 旧版本)留在库中,不影响检索(全链路过滤 deleted=0),仅表膨胀;如需物理清理可 `DELETE FROM ... WHERE deleted=1`(一次性运维,非代码问题)。
- 6432 旧 clean 行同理(param_id 已逻辑删,检索层 detail 接口按 deleted=0 过滤,不影响展示)。

## 修复清单(本任务)

- persistVersions/persistParams 同名去重(防上游再重复)。
- AiParamCleaner 空值防线(空值不落 clean)。
- 未改「先清后插」语义(逻辑删是既有全局约定,删除物理化会影响审计/回滚,不在本任务动)。
